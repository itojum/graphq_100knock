fragment UserFields on BaseUser {
  userId
  name
  email
}

query ExampleQuery($userId: ID!) {
  user(userId: $userId) {
    ...UserFields
    followerCount
    followers {
      ...UserFields
    }
  }
}
